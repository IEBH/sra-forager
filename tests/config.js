/*
* Used for testing purposes only. Driver configurations, especially those with apiKeys should
* be passed directly into the forageCitations function by an external client.
*/
const config = {
  drivers: {
    scopus: {
      apiKey: '',
    }
  }
}

module.exports = config;